This project is just a demo hơ to use git
// git init khoi tao
// git status kiem tra trang thai
// git add them vao
// git commit dong goi cac tep da add vao 1 hop vaf dan cho no 1 cai ten

b2 
// git log : show het các file đã commit có mã dán
// git show: lấy mã dán sau khi git log và git show tên mã dán từ git log
// git diff // different de xem noi dung voi fle midifile

b3 
// working directory : la cac file dang lam viec(khi chu mau do)
// staging area : la cac file sau khi add (khi chu mau xanh)
// git repository: sau khi commit

b4 
// git checkout(git restore): bo di nhung thay doi trong file da viet khi chua add
// git reset(git restore --staged<file>): chuyển trang thái từ sau khi add thành chưa add 